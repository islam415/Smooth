using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Obstacle : MonoBehaviour
{
    //private void OnTriggerEnter(Collider other)
    //{
    //    if (other.tag=="Player")
    //    {
    //        int count = other.transform.parent.GetChild(0).childCount;
    //        Destroy(other.transform.parent.GetChild(0).GetChild(count));
    //    }
    //}
}
